# Default number of objects per page
PER_PAGE = 9

# Django secret key
SECRET_KEY = 'CHANGE-ME'

# Simple JWT SECRET_KEY
SECRET_KEY_JWT = 'CHANGE-ME'

# 0 = False - 1 = True
DEBUG = 0

# 0 = False - 1 = True
SELENIUM_HEADLESS = 1

# Database settings
# Sqlite
DATABASE_ENGINE = 'django.db.backends.sqlite3'
DATABASE_NAME = "./db.sqlite3"

# Postgres (If you use postgres, uncomment the next two lines)
DATABASE_ENGINE='django.db.backends.postgresql'
DATABASE_NAME="datavoluntiers"

DATABASE_USER = "admin"
DATABASE_PASSWORD = "admin"
DATABASE_HOST = "127.0.0.1"
DATABASE_PORT = "5432"

# Comma separated values
ALLOWED_HOSTS = '0.0.0.0, localhost, app-voluntiers.com'
CSRF_TRUSTED_ORIGINS = 'https://localhost, https://app-voluntiers.com'
CORS_ALLOWED_ORIGINS = 'http://127.0.0.1:8000,'